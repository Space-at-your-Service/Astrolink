from django.apps import AppConfig


class InventoryConfig(AppConfig):

    """ InventoryConfig
    Represents the inventory app of the website

    @field name (str) : internal name for the Django app
    """

    name = 'inventory'